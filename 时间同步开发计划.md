# 全局时间同步 — 开发计划

## 1. 需求概述

### 当前问题

现版本中，每个玩家的 RimWorld 实例完全独立运行，游戏时间（倍速、暂停状态）互不通信。玩家 A 按空格暂停，玩家 B 的游戏仍在运行；管理员调整倍速，只影响自己。

### 目标

所有已连接客户端共享同一时间流速和暂停状态：

- 任一客户端（Admin）暂停 → 所有客户端同步暂停
- 任一客户端（Admin）调整倍速 → 所有客户端同步切换倍速
- 事件触发时间效果 → 所有客户端同步响应
- 新玩家加入 → 自动对齐当前世界的时间状态

### 核心原则

**Server 是唯一时间源（Source of Truth）**。客户端不自主决定倍速/暂停，完全由 Server 下发指令控制。

---

## 2. 设计难点：Tick 级累积误差

### 2.1 问题分析

仅同步倍速变更事件，**无法消除网络延迟带来的 tick 级累积误差**：

```
时间线: ────────────────────────────────────────────────────►

Admin 切换 3x                      广播到达 B
    │                                  │
    ├──── 延迟 200ms ────┤              │
    │                    │              │
Client A:  [1x 运行]     [3x 运行]     [3x 运行]
Client B:  [1x 运行]     [1x 运行]     [3x 运行]
                          ↑
                    200ms 内 B 少跑了 200ms × 2x = 400ms 等效 tick
```

每次速度变更都产生一次偏差，长期运行后累积误差可达数秒甚至数十秒。对于 RimWorld 这种合作建造类游戏，虽非竞技级精度要求，但偏差过大仍会导致：

- 同步会话（PM_Synchronous）中双方看到的游戏世界时间不一致
- 贸易、Raid 等操作时双方在略微不同的 tick 下执行，产生逻辑歧义
- 长时运行（数小时）后误差不可忽视

### 2.2 设计策略：两级同步体系

**不追求毫秒级绝对一致**，而是通过 **「事件广播 + 周期性心跳校准 + 弹性追帧」** 两级体系，将误差控制在可接受范围内（±30 ticks 以内）：

| 层级 | 机制 | 触发频率 | 作用 |
|------|------|----------|------|
| **粗粒度** | 倍速/暂停变更广播 | 事件驱动 | 所有人按相同速度运行，误差不扩大 |
| **细粒度** | 周期性 Tick 心跳 | 每 5 秒 | 检测并修正累积误差，落后客户端快速追帧 |

### 2.3 心跳校准流程

```
Server 每 5 秒 计算当前权威 tick → 广播 PKT_TickHeartbeat

Client 收到心跳:
  ├─ drift = ServerTick - LocalTick
  ├─ |drift| ≤ TOLERANCE (30 ticks) → 忽略，误差在可接受范围
  ├─ drift > +30 (客户端落后) → 临时加速至 1.5x~3x 追帧，追上后恢复
  └─ drift < -30 (客户端超前) → 插入短暂空闲 tick 等待对齐
```

### 2.4 为什么不用 Lockstep（锁步同步）

RTS 常用的 Lockstep 要求：
- 所有客户端逻辑完全确定性
- 逐帧等待所有玩家输入
- 一人卡顿全员等待

这需要对 RimWorld 引擎做彻底改造，工程量巨大且不适合 RimWorld 的合作模式。**弹性校准是最务实的方案**。

---

## 3. 改动范围总览

| 改动文件 | 归属 | 类型 | 可行性 |
|----------|------|------|--------|
| `RTShared.dll` | 共享库 | 新增 `FL_GameTime` 数据类 | ❌ 需 DLL 源码 |
| `RTNetwork.dll` | 共享库 | 新增 `PacketHeader.GameTime`、`PKT_Time`、`PKT_TickHeartbeat` | ❌ 需 DLL 源码 |
| `RTClient.dll` | 客户端 | Harmony Patch 挂钩 TickManager + 弹性追帧 | ❌ 需 DLL 源码 |
| `Source/Server/Core/Master.cs` | 服务端 | 新增全局时间状态字段 + Tick 计数器 | ✅ 可直接改 |
| `Source/Server/Core/Program.cs` | 服务端 | 新增时间状态加载 + 启用心跳定时器 | ✅ 可直接改 |
| `Source/Server/Managers/GlobalDataManager.cs` | 服务端 | 登入时下发时间状态 | ✅ 可直接改 |
| `Source/Server/Managers/TickManager.cs` | 服务端 | **新建**，Server 侧权威 Tick 计时器 | ✅ 直接新建 |
| `Source/Server/PacketManagers/PM_Time.cs` | 服务端 | **新建**，处理时间同步包 + 心跳广播 | ✅ 直接新建 |
| `Source/Server/Commands/CMD_Time.cs` | 服务端 | **新建**，Admin 控制台命令 | ✅ 直接新建 |
| `Source/Server/PacketManagers/PM_Events.cs` | 服务端 | 事件触发时联动时间操作 | ✅ 可直接改 |

---

## 4. 共享库改动（RTShared.dll + RTNetwork.dll）

> ⚠️ 这两个 DLL 是编译后的二进制文件，仓库中无源码。需要联系原作者获取源码，或反编译后修改并重新编译。

### 4.1 新增 `FL_GameTime` 数据类

**文件位置**：`RTShared.dll` 中新建

```csharp
public class FL_GameTime
{
    public float  TimeSpeed  { get; set; } = 1.0f;  // 当前倍速: 1.0 / 2.0 / 3.0
    public bool   IsPaused   { get; set; } = false;  // 是否暂停
    public string PausedBy   { get; set; } = null;   // 暂停来源: Admin 用户名 或 事件名称
    public long   ServerTick { get; set; } = 0;      // 服务端权威 tick 计数
    public long   TickStartTimestamp { get; set; }   // 上次 tick 基准时间戳 (DateTime.Ticks)
}
```

### 4.2 新增 `PacketHeader.GameTime` 枚举值

**文件位置**：`RTNetwork.dll` 中的 `PacketHeader` 枚举

```csharp
public enum PacketHeader
{
    // ... 已有枚举值 ...
    GameTime,   // 新增：倍速/暂停变更 + 心跳
}
```

### 4.3 新增 `PKT_Time` 包结构

**文件位置**：`RTNetwork.dll` 中新建

```csharp
public class PKT_Time
{
    public enum TimeStepMode
    {
        SetSpeed,      // 设置倍速
        Pause,         // 暂停
        Resume,        // 恢复
        TickHeartbeat  // 周期性 tick 心跳（细粒度校准）
    }

    public TimeStepMode StepMode    { get; set; }
    public float        Speed       { get; set; }  // 倍速值（SetSpeed 时使用）
    public string       PausedBy    { get; set; }  // 暂停来源（Pause 时使用）
    public long         ServerTick  { get; set; }  // 权威 tick 值（TickHeartbeat 时使用）
}
```

### 4.4 扩展 `PKT_ServerGlobalData`

**文件位置**：`RTNetwork.dll` 中的 `PKT_ServerGlobalData`

新增字段，用于新玩家登入时拿到当前时间状态：

```csharp
public class PKT_ServerGlobalData
{
    // ... 已有字段 ...
    public float TimeSpeed  { get; set; }  // 新增
    public bool  IsPaused   { get; set; }  // 新增
    public long  ServerTick  { get; set; }  // 新增：登入时的权威 tick
}
```

---

## 5. 服务端改动（可直接改源码）

### 5.1 `Master.cs` — 新增全局时间状态

**文件路径**：`Source/Server/Core/Master.cs`

在 `Master` 类中新增静态字段：

```csharp
// 时间同步
public static float  GlobalTimeSpeed    { get; set; } = 1.0f;
public static bool   GlobalTimePaused   { get; set; } = false;
public static string GlobalTimePausedBy { get; set; } = null;
public static long   GlobalServerTick   { get; set; } = 0;
public static long   TickBaseTimestamp  { get; set; }  // 上次重算 tick 基准的 DateTime.Ticks

// 心跳间隔（毫秒），可通过配置调整
public static int    TickHeartbeatIntervalMs { get; set; } = 5000;
```

### 5.2 `Program.cs` — 启动时加载时间状态 + 启用心跳定时器

**文件路径**：`Source/Server/Core/Program.cs`

在 `SetPaths()` 方法中新增：
```csharp
FL_GameTime.SavePath = Path.Combine(Master.AssetsPath, "GameTime.json");
```

在 `LoadFiles()` 方法中新增：
```csharp
FL_GameTime savedTime = Serializer.SerializeFromFile<FL_GameTime>(FL_GameTime.SavePath);
if (savedTime != null)
{
    Master.GlobalTimeSpeed = savedTime.TimeSpeed;
    Master.GlobalTimePaused = savedTime.IsPaused;
    Master.GlobalTimePausedBy = savedTime.PausedBy;
    Master.GlobalServerTick = savedTime.ServerTick;
}
Master.TickBaseTimestamp = DateTime.UtcNow.Ticks;
```

在 `Start()` 或启动完成后新增心跳定时器：
```csharp
// 启动 tick 心跳定时器
Task.Run(() => TickManager.StartHeartbeat());
```

### 5.3 新建 `TickManager.cs` — Server 侧权威 Tick 计时器

**文件路径**：`Source/Server/Managers/TickManager.cs`

**职责**：
1. Server 维护一个权威 tick 计数器，根据当前倍速和真实时间流逝推进
2. 周期性（每 5 秒）通过 PM_Time 广播心跳给所有客户端

```csharp
namespace GameServer.Managers
{
    public static class TickManager
    {
        // RimWorld 默认 60 ticks/秒 at 1x speed
        public const int TicksPerSecond = 60;

        public static void StartHeartbeat()
        {
            while (true)
            {
                Thread.Sleep(Master.TickHeartbeatIntervalMs);

                if (Master.GlobalTimePaused) continue;

                AdvanceTick();
                PM_Time.BroadcastTickHeartbeat();
                SaveTimeState();
            }
        }

        public static void AdvanceTick()
        {
            long now = DateTime.UtcNow.Ticks;
            double elapsedSeconds = (now - Master.TickBaseTimestamp) / (double)TimeSpan.TicksPerSecond;
            long tickAdvance = (long)(elapsedSeconds * TicksPerSecond * Master.GlobalTimeSpeed);
            Master.GlobalServerTick += tickAdvance;
            Master.TickBaseTimestamp = now;
        }

        public static void SaveTimeState()
        {
            FL_GameTime state = new FL_GameTime
            {
                TimeSpeed = Master.GlobalTimeSpeed,
                IsPaused = Master.GlobalTimePaused,
                PausedBy = Master.GlobalTimePausedBy,
                ServerTick = Master.GlobalServerTick,
                TickStartTimestamp = Master.TickBaseTimestamp
            };
            Serializer.SerializeToFile(FL_GameTime.SavePath, state);
        }
    }
}
```

### 5.4 `GlobalDataManager.cs` — 登入时下发时间状态

**文件路径**：`Source/Server/Managers/GlobalDataManager.cs`

在 `SendServerGlobalData()` 方法的 `globalData` 赋值区域新增：

```csharp
globalData.TimeSpeed  = Master.GlobalTimeSpeed;
globalData.IsPaused   = Master.GlobalTimePaused;
globalData.ServerTick = Master.GlobalServerTick;
```

### 5.5 新建 `PM_Time.cs` — 时间同步包处理器

**文件路径**：`Source/Server/PacketManagers/PM_Time.cs`

**功能**：接收客户端的时间变更请求，校验权限后广播；周期性广播 tick 心跳。

```
PM_Time 继承 PM_Base

[HandlesPacket(PacketHeader.GameTime)]
Receive(client, bytes, header)
  → 反序列化 PKT_Time
  → 根据 StepMode 路由:
      SetSpeed → HandleSetSpeed(client, data)
      Pause    → HandlePause(client, data)
      Resume   → HandleResume(client, data)

HandleSetSpeed:
  → 校验: 仅 Admin 可操作
  → 先结算当前 tick（AdvanceTick）
  → 更新: Master.GlobalTimeSpeed = data.Speed
  → 重置: Master.TickBaseTimestamp = DateTime.UtcNow.Ticks
  → 持久化: 保存到 FL_GameTime 文件
  → 广播: ServerNetwork.SendPacketToAllClients(PacketHeader.GameTime, data)

HandlePause:
  → 校验: 仅 Admin 或 Event 可操作
  → 先结算当前 tick（AdvanceTick）
  → 更新: Master.GlobalTimePaused = true
  → 记录: Master.GlobalTimePausedBy = data.PausedBy
  → 持久化 + 广播

HandleResume:
  → 校验: 仅 Admin 或 Event 可操作
  → 更新: Master.GlobalTimePaused = false
  → 重置: Master.TickBaseTimestamp = DateTime.UtcNow.Ticks（暂停期间不计 tick）
  → 持久化 + 广播

BroadcastTickHeartbeat:  // 定时器调用，非客户端请求触发
  → AdvanceTick() 已在 TickManager 中调用
  → 构造 PKT_Time(TickHeartbeat, ServerTick=Master.GlobalServerTick)
  → ServerNetwork.SendPacketToAllClients(PacketHeader.GameTime, data)
```

### 5.6 新建 `CMD_Time.cs` — 管理员控制台命令

**文件路径**：`Source/Server/Commands/CMD_Time.cs`

| 命令 | 参数 | 作用 |
|------|------|------|
| `time speed 1` | 倍速值 | 设置全局倍速为 1x |
| `time speed 2` | 倍速值 | 设置全局倍速为 2x |
| `time speed 3` | 倍速值 | 设置全局倍速为 3x |
| `time pause` | 无 | 暂停全局时间 |
| `time resume` | 无 | 恢复全局时间 |
| `time status` | 无 | 查看当前时间状态（Tick / Speed / Paused） |

### 5.7 `PM_Events.cs` — 事件联动时间

**文件路径**：`Source/Server/PacketManagers/PM_Events.cs`

在 `SendEvent()` 方法中，事件数据附带时间效果时，触发时间同步：

```csharp
if (事件包含时间效果)
{
    PKT_Time timeData = new PKT_Time();
    timeData.StepMode = 事件指定的 StepMode;
    timeData.Speed    = 事件指定的倍速;
    timeData.PausedBy = 事件名称;
    ServerNetwork.SendPacketToAllClients(PacketHeader.GameTime, timeData);

    // 同步更新 Master 状态
    Master.GlobalTimeSpeed  = timeData.Speed;
    Master.GlobalTimePaused = timeData.StepMode == TimeStepMode.Pause;
    Master.GlobalTimePausedBy = timeData.PausedBy;
}
```

---

## 6. 客户端改动（RTClient.dll）

> ⚠️ RTClient.dll 是编译后的二进制文件，仓库中无源码。需要联系原作者获取源码，或反编译后修改并重新编译。

### 6.1 `SyncedTime` 静态状态持有类

```csharp
static class SyncedTime
{
    public static float TimeSpeed  = 1.0f;
    public static bool  IsPaused   = false;
    public static long  ServerTick = 0;       // 上次心跳的权威 tick
    public static long  LocalTick  = 0;       // 本地当前 tick

    // 弹性追帧参数
    public const long TOLERANCE = 30;           // ±30 ticks 容忍区间
    public static float CatchUpMultiplier = 1.0f; // 追帧倍率（1.0 = 正常）
}
```

### 6.2 Harmony Patch 挂钩 TickManager

**Patch 1：拦截速度设置**

```csharp
[HarmonyPatch(typeof(TickManager), "get_TickRateMultiplier")]
static class TimeSpeedSync
{
    static void Postfix(ref float __result)
    {
        // 应用弹性追帧倍率
        __result *= SyncedTime.CatchUpMultiplier;
    }
}
```

**Patch 2：拦截暂停状态**

```csharp
[HarmonyPatch(typeof(TickManager), "set_Paused")]
static class TimePauseSync
{
    static bool Prefix(ref bool value)
    {
        value = SyncedTime.IsPaused;
        return true;
    }
}
```

**Patch 3：追踪本地 Tick（可选，如有 Tick 回调）**

```csharp
[HarmonyPatch(typeof(TickManager), "DoSingleTick")]
static class TickTracker
{
    static void Postfix()
    {
        SyncedTime.LocalTick++;
    }
}
```

### 6.3 接收 `PKT_Time` 并处理

```csharp
void OnTimePacketReceived(PKT_Time packet)
{
    switch (packet.StepMode)
    {
        case TimeStepMode.SetSpeed:
            SyncedTime.TimeSpeed = packet.Speed;
            SyncedTime.CatchUpMultiplier = 1.0f; // 重置追帧倍率
            SyncedTime.ServerTick = packet.ServerTick;
            Find.TickManager.TickRateMultiplier = packet.Speed;
            break;

        case TimeStepMode.Pause:
            SyncedTime.IsPaused = true;
            SyncedTime.ServerTick = packet.ServerTick;
            Find.TickManager.Paused = true;
            break;

        case TimeStepMode.Resume:
            SyncedTime.IsPaused = false;
            SyncedTime.CatchUpMultiplier = 1.0f;
            SyncedTime.ServerTick = packet.ServerTick;
            Find.TickManager.Paused = false;
            break;

        case TimeStepMode.TickHeartbeat:
            AdjustTickDrift(packet.ServerTick);
            break;
    }
}
```

### 6.4 弹性追帧逻辑

```csharp
void AdjustTickDrift(long serverTick)
{
    long drift = serverTick - SyncedTime.LocalTick;

    if (Math.Abs(drift) <= SyncedTime.TOLERANCE)
    {
        // 误差在容忍范围内，不做任何调整
        SyncedTime.CatchUpMultiplier = 1.0f;
        return;
    }

    if (drift > 0)
    {
        // 客户端落后：渐进加速追帧
        // 追帧倍率 = min(3.0, 1.0 + drift / 100.0)  → 最多 3x 加速
        long catchUpTicks = drift / 2;  // 用一半的误差量来追，避免突然跳变
        SyncedTime.CatchUpMultiplier = Math.Min(3.0f, 1.0f + catchUpTicks / 100.0f);
    }
    else
    {
        // 客户端超前：插入空闲等待
        // 不直接设置 CatchUpMultiplier < 1.0（RimWorld 引擎可能不支持）
        // 改为在每个 tick 中插入额外等待
        SyncedTime.CatchUpMultiplier = 1.0f;
        SyncedTime.InsertIdleTicks = Math.Abs(drift);
    }
}
```

### 6.5 登入时初始化时间状态

收到 `PKT_ServerGlobalData` 时，同步设置本地时间状态：

```csharp
void OnGlobalDataReceived(PKT_ServerGlobalData data)
{
    // ... 已有的初始化逻辑 ...

    SyncedTime.TimeSpeed  = data.TimeSpeed;
    SyncedTime.IsPaused   = data.IsPaused;
    SyncedTime.ServerTick = data.ServerTick;
    SyncedTime.LocalTick  = data.ServerTick;  // 登入时以 Server 为准
    Find.TickManager.TickRateMultiplier = data.TimeSpeed;
    Find.TickManager.Paused = data.IsPaused;
}
```

---

## 7. 同步流程

### 7.1 Admin 暂停

```
Admin 按空格
  → 客户端 A 构造 PKT_Time(Pause, PausedBy="Admin用户名")
  → 发送到 Server
  → PM_Time.HandlePause()
    → TickManager.AdvanceTick() 结算当前 tick
    → Master.GlobalTimePaused = true
    → 广播 PKT_Time(Pause, ServerTick=结算后的 tick)
      → 所有客户端: Find.TickManager.Paused = true
      → 所有客户端: SyncedTime.ServerTick = 对齐 tick
```

### 7.2 Admin 调整倍速

```
Admin 切换 3x
  → 客户端 A 构造 PKT_Time(SetSpeed, Speed=3.0f)
  → 发送到 Server
  → PM_Time.HandleSetSpeed()
    → TickManager.AdvanceTick() 结算当前 tick
    → Master.GlobalTimeSpeed = 3.0f
    → 广播 PKT_Time(SetSpeed, 3.0f, ServerTick=结算后的 tick)
      → 所有客户端: TickRateMultiplier = 3.0f
      → 所有客户端: SyncedTime.ServerTick = 对齐 tick
```

### 7.3 周期性 Tick 心跳 + 弹性追帧

```
Server TickManager 每 5 秒:
  → AdvanceTick() 计算 elapsed * speed * 60 ticks/sec
  → 广播 PKT_Time(TickHeartbeat, ServerTick=1234567)

Client B 收到心跳 (本地 tick=1234480):
  → drift = 1234567 - 1234480 = +87 (落后 87 ticks)
  → 87 > 30 TOLERANCE → 触发追帧
  → CatchUpMultiplier = min(3.0, 1.0 + 43/100) = 1.43x
  → 本地以 1.43x 倍速运行，逐步追上

5 秒后下次心跳:
  → drift = 1235700 - 1235690 = +10 (误差已缩小到容忍范围)
  → CatchUpMultiplier = 1.0 (恢复正常)
```

### 7.4 事件触发时间效果

```
Event 触发（如"寒冬来临"需要暂停）
  → PM_Events.SendEvent() 发送事件后
  → 构造 PKT_Time(Pause, PausedBy="寒冬来临")
  → ServerNetwork.SendPacketToAllClients(PKT_Time(Pause))
  → 同时更新 Master 状态
```

### 7.5 新玩家加入

```
新玩家登录
  → PM_Login.PostLogin()
  → GlobalDataManager.SendServerGlobalData()
    → PKT_ServerGlobalData.TimeSpeed  = 3.0f
    → PKT_ServerGlobalData.IsPaused   = true
    → PKT_ServerGlobalData.ServerTick = 1234567
  → 客户端收到后:
    → LocalTick = ServerTick = 1234567
    → Find.TickManager.TickRateMultiplier = 3.0f
    → Find.TickManager.Paused = true
  → 后续心跳会自动维持对齐
```

---

## 8. 开发顺序

### 阶段一：建立协议（共享库）

1. `RTShared.dll` 中新增 `FL_GameTime` 类
2. `RTNetwork.dll` 中新增 `PacketHeader.GameTime` 枚举值
3. `RTNetwork.dll` 中新增 `PKT_Time` 包结构（含 TickHeartbeat 模式）
4. `RTNetwork.dll` 中扩展 `PKT_ServerGlobalData`（新增 TimeSpeed / IsPaused / ServerTick）

### 阶段二：服务端实现

1. `Master.cs` 新增全局时间字段 + Tick 计数器
2. `Program.cs` 新增时间状态加载 + 启动心跳定时器
3. 新建 `TickManager.cs` Server 侧权威 Tick 计时器
4. `GlobalDataManager.cs` 登入时下发时间状态 + ServerTick
5. 新建 `PM_Time.cs` 处理时间同步包 + 心跳广播
6. 新建 `CMD_Time.cs` 控制台命令
7. `PM_Events.cs` 事件联动时间

### 阶段三：客户端实现

1. 新建 `SyncedTime` 静态状态持有类（含追帧参数）
2. Harmony Patch 挂钩 `TickManager.get_TickRateMultiplier`（追帧倍率）
3. Harmony Patch 挂钩 `TickManager.set_Paused`
4. Harmony Patch 挂钩 `TickManager.DoSingleTick`（追踪本地 tick）
5. 接收 `PKT_Time` 并应用（含弹性追帧 AdjustTickDrift）
6. 登入时初始化时间状态

### 阶段四：测试验证

1. 单 Admin 暂停/恢复 → 验证所有客户端同步
2. 单 Admin 切换倍速 → 验证所有客户端同步
3. 心跳追帧：A 客户端 3x 运行 5 分钟后 B 加入 → 验证 B 快速追帧对齐
4. 新玩家加入 → 验证时间状态对齐
5. 事件触发时间效果 → 验证所有客户端同步
6. 极端场景：连续快速切换暂停/恢复 → 验证 tick 结算正确
7. 长时运行（1 小时）→ 验证 tick 漂移始终在 ±30 容忍范围内

---

## 9. 风险与注意事项

### 9.1 DLL 源码依赖

`RTClient.dll`、`RTNetwork.dll`、`RTShared.dll` 在仓库中仅以编译后二进制形式存在。**阶段一和阶段三依赖这些 DLL 的源码**。需要：
- 联系项目原作者（Byte-Nova）获取源码
- 或使用反编译工具（如 dnSpy）反编译后修改

### 9.2 追帧引发的 "时间加速" 视觉问题

客户端落后时以 1.5x~3x 追帧，短时间内画面会加速。解决方式：
- 追帧倍率上限 3x，与 RimWorld 已有的最高倍速一致，玩家不会感到异常
- 追帧在后台静默执行，不显示追帧状态 UI（避免用户困惑）

### 9.3 客户端超前场景

客户端超前（drift < -30）意味着本地跑得比 Server 快。这通常发生在 Server 广播延迟时。处理方式：
- 插入空闲 tick（暂停 TickManager 的 DoSingleTick 若干帧）
- 如果超前量很大（> 300 ticks），直接在 Harmony Patch 中跳过 tick 执行

### 9.4 暂停期间的心跳

暂停期间 TickManager 心跳线程跳过 AdvanceTick 和广播，避免无效网络开销。恢复时重置 TickBaseTimestamp。

### 9.5 心跳间隔的选择

- 5 秒间隔：在 3x 倍速下，5 秒 = 900 ticks 最大偏差。如果上一次心跳刚好在速度变更前发出，偏差可能达到 900 + 广播延迟。
- 如果偏差过大，可降低间隔至 3 秒（3x 下 540 ticks），但会增加网络负担。
- 推荐保留 5 秒，配合 TOLERANCE=30 和追帧机制，足够应对。

### 9.6 玩家自主暂停的冲突

普通玩家按空格暂停会被 Harmony Patch 拦截并忽略，转为由 Server 控制。**普通玩家无法自主暂停**——这是一个设计决策，需与产品确认。

替代方案：允许普通玩家发送"暂停请求"，Server 收到后广播给所有人。

### 9.7 离线玩家

离线玩家的时间状态需要在下一次登录时对齐。通过 `PKT_ServerGlobalData` 在登入时下发当前时间状态 + ServerTick 即可解决。

---

## 10. 文件改动清单

```
RTShared.dll
  └── 新增 FL_GameTime.cs              // 时间状态数据类（含 ServerTick）

RTNetwork.dll
  ├── 修改 PacketHeader 枚举             // 新增 GameTime
  ├── 新增 PKT_Time.cs                  // 时间同步包结构（含 TickHeartbeat 模式）
  └── 修改 PKT_ServerGlobalData.cs      // 扩展 TimeSpeed / IsPaused / ServerTick

RTClient.dll
  ├── 新增 SyncedTime.cs                // 静态同步状态 + 追帧参数
  └── 修改 TimeSyncPatch.cs            // Harmony Patch + 弹性追帧 AdjustTickDrift

Source/Server/Core/
  ├── Master.cs                         // 新增 GlobalTimeSpeed / GlobalTimePaused / GlobalServerTick / TickHeartbeatIntervalMs
  └── Program.cs                        // 新增 GameTime.json 加载 + 心跳定时器启动

Source/Server/Managers/
  ├── TickManager.cs                    // 【新建】Server 侧权威 Tick 计时器 + 心跳定时器
  └── GlobalDataManager.cs             // 登入时下发 TimeSpeed / IsPaused / ServerTick

Source/Server/PacketManagers/
  ├── PM_Time.cs                        // 【新建】时间同步包处理器 + BroadcastTickHeartbeat
  └── PM_Events.cs                      // 事件联动时间操作

Source/Server/Commands/
  └── CMD_Time.cs                       // 【新建】Admin 控制台时间命令
```